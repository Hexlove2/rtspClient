{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make",  // 依然使用make进行构建
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/build"  // 进入build目录执行make
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}